# OrgMirror — AI Multi-Agent 组织效率审计框架

当设计、审查或优化 multi-agent 系统时，使用以下效率审计框架：

## 核心效率指标

1. **信息增量 (info_delta)**：每个 agent 的输出 vs 输入的新增信息比例
   - < 15% = 传话筒（该 agent 只是复述上游，建议移除）
   - 15-40% = 辅助角色
   - > 40% = 核心贡献者

2. **决策影响 (decision_impact)**：agent 产出被下游采用的程度
   - < 30% = 无效节点（说了等于没说）
   - 30-70% = 有影响但非决定性
   - > 70% = 关键决策者

3. **管理层开销**：非执行层 agent 消耗的总 token 占比
   - > 40% = 过度管理（来自《大厂人才》结论：互联网大厂43%管理层token是浪费的）

4. **可跳过性 (skip_score)**：综合以上指标
   - > 60% 的 agent 建议移除

## Agent 设计原则（来自大厂真实教训）

### 避免传话筒模式
- 不要设计只做"审批转发"的 agent（如 VP 说"方向对了继续"）
- 每个 agent 必须有明确的信息增量要求
- 审查/评审 agent 必须输出量化评分，而非"同意/不同意"

### 避免赛马浪费
- 多个 agent 做同一件事时，先评估重复度（>70% 相似度 = 浪费）
- 如需竞争机制，确保评审标准透明可追溯

### 避免中台延迟
- 共享服务层不应成为瓶颈
- 基础设施 agent 应该是被动响应而非主动审批

## 组织架构参考

项目提供 4 套可执行的大厂架构（字节8agent/阿里13agent/腾讯12agent/华为10agent），可用于对比测试。

```bash
# 用同一个任务跑四套架构对比
orgmirror "你的任务" --compare
```

详见：https://github.com/Thinkwriting/orgmirror
